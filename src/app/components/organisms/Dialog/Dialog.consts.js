import {
    ALIGNMENTS,
    DIRECTIONS,
    EASINGS,
    ELEVATIONS,
} from '../../../utils/constants';

export const DIALOG_ALIGNMENTS = ALIGNMENTS;

export const DIALOG_DIRECTIONS = DIRECTIONS;

export const DIALOG_ELEVATIONS = ELEVATIONS;

export const DIALOG_EASINGS = EASINGS;
