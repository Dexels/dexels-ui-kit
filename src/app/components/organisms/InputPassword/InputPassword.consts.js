import { grey10, grey100 } from '../../../styles/colors/colors';
import { INPUT_VARIANTS } from '../../../utils/constants';

export const INPUT_PASSWORD_THEME = {
    visibilitySwitchColorDefault: grey100,
    visibilitySwitchColorDisabled: grey10,
};

export const INPUT_PASSWORD_VARIANTS = INPUT_VARIANTS;
