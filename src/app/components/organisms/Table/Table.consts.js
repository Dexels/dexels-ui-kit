import { ELEVATIONS } from '../../../utils/constants';
import { PAGINATOR_PAGE_SIZES } from './Paginator/Paginator.consts';

export const TABLE_ELEVATIONS = ELEVATIONS;

export const TABLE_PAGE_SIZES = PAGINATOR_PAGE_SIZES;
