import * as colors from '../../../styles/colors/colors';
import { DIRECTIONS } from '../../../utils/constants';

export const TEXT_WITH_OPTIONAL_ICON_COLORS = colors;

export const TEXT_WITH_OPTIONAL_ICON_DIRECTIONS = DIRECTIONS;
