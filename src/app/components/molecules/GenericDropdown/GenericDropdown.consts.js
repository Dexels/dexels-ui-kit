import {
    blue100,
    grey10,
    grey100,
} from '../../../styles/colors/colors';

export const GENERIC_DROPDOWN_THEME = {
    activeInputColor: blue100,
    backgroundColor: 'transparent',
    disabledColor: grey10,
    dividerColor: grey100,
};

export default GENERIC_DROPDOWN_THEME;
