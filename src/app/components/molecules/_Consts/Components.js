import styled from 'styled-components';

/* Used in several molecules where icons are used */
export const IconWrapperComponent = styled.div`
    flex: 0 0 auto;
    order: 1;
    margin: 0 6px 0 0;
`;

/* Used in several molecules where icons + text are used */
export const TextComponent = styled.p`
    flex: 0 0 auto;
    order: 2;
`;
