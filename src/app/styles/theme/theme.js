/* eslint-disable sort-keys */
// The sort keys ESLint rule is disabled in this file so we can group our variables in any way we like
import { purple100, sl10 } from '../colors/colors';

const theme = {
    // Global theming
    primaryTextColor: purple100,
    // Button theming
    buttonPrimaryColor: purple100,
    buttonDisabledColor: sl10,
    buttonTextColor: 'white',
    buttonSmallBorderRadius: '16px',
    buttonSmallHeight: '32px',
    buttonLargeBorderRadius: '24px',
    buttonLargeHeight: '48px',
};

export default theme;
