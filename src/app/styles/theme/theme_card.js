import {
    white,
} from '../colors/colors';

const themeCard = {
    card: {
        backgroundColor: white,
        borderRadius: '4px',
        padding: '8px',
    },
};

export default themeCard;
