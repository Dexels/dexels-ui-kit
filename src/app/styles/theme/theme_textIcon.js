import {
    sl50,
    white,
} from '../colors/colors';
import defaultTextStyle from './defaultTextStyle';

const themeTextIcon = {
    textIcon: {
        backgroundColor: sl50,
        borderRadius: '50px',
        colorPrimary: white,
        size: '24px',
    },
    textStyles: {
        textIcon: {
            fontFamily: 'sans-serif',
            fontSize: '24px',
            fontWeight: '600',
        },
    },
};

export default themeTextIcon;
